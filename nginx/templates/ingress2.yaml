# Ingress는 values.yaml에서 enabled: false로 설정되어 있어서 생성되지 않습니다.
# 만약 ingress를 활성화하려면 values.yaml에서 ingress.enabled를 true로 변경해야 합니다.
# 
# 활성화된 경우의 예시:
# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: nginx
#   labels:
#     app.kubernetes.io/name: nginx
#     app.kubernetes.io/instance: nginx
#     app.kubernetes.io/version: "1.16.0"
#     app.kubernetes.io/managed-by: Helm
#   annotations: {}
# spec:
#   rules:
#     - host: "chart-example.local"
#       http:
#         paths:
#           - path: /
#             pathType: ImplementationSpecific
#             backend:
#               service:
#                 name: nginx
#                 port:
#                   number: 80
